"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ReducerRenderer = void 0;
const utils_1 = require("../../../utils/utils");
class ReducerRenderer {
    render(item) {
        return this.toPlantUml(item);
    }
    toPlantUml(item) {
        const fileName = utils_1.getFileName(item.filePath);
        const srcText = fileName ? `src: ${fileName}` : '';
        const diagramContent = `interface "${item.name}" << (R,#0077FF) reducer >> {
            ${srcText}
            --
        }

        `;
        return diagramContent;
    }
}
exports.ReducerRenderer = ReducerRenderer;
