import { ItemRenderer } from '../../../core/renderers/item.renderer';
import { Action } from '../../models/action.model';
export declare class ActionRenderer implements ItemRenderer {
    render(item: Action): string;
    toPlantUml(item: Action): string;
    private typeLiteralToText;
    private callExpressionToText;
    private createActionArgsToText;
}
