import { ConvertContextFactory } from '../core/converters';
import { Output } from '../core/outputs/output';
import { Renderer } from '../core/renderers';
import { GeneratorOptions } from './generator-options';
export declare class GeneratorService {
    private readonly convertFactories;
    private readonly renderer;
    private readonly outputs;
    options: GeneratorOptions;
    constructor(convertFactories: ConvertContextFactory[], renderer: Renderer, outputs: Output[], options?: GeneratorOptions);
    generate(filesPattern: string): Promise<void>;
    private createTsProgram;
    private convert;
    private saveConvertResult;
    private render;
    private transform;
}
